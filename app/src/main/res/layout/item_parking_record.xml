<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginHorizontal="16dp"
    android:layout_marginVertical="8dp"
    app:cardCornerRadius="12dp"
    app:cardElevation="4dp">

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="12dp">

        <!-- 1. 주차 사진 (왼쪽) -->
        <ImageView
            android:id="@+id/iv_record_image"
            android:layout_width="80dp"
            android:layout_height="80dp"
            android:background="#EEEEEE"
            android:scaleType="centerCrop"
            android:src="@android:drawable/ic_menu_camera"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent" />

        <!-- 2. 주차 시간 (우측 상단 날짜) -->
        <TextView
            android:id="@+id/tv_record_time"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginStart="12dp"
            android:textColor="#888888"
            android:textSize="12sp"
            app:layout_constraintStart_toEndOf="@id/iv_record_image"
            app:layout_constraintTop_toTopOf="parent"
            tools:text="2025.11.22 14:30" />

        <!-- 3. 층수/위치 정보 (굵은 제목) -->
        <TextView
            android:id="@+id/tv_record_floor"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginTop="4dp"
            android:layout_marginEnd="16dp"
            android:textColor="#000000"
            android:textSize="16sp"
            android:textStyle="bold"
            app:layout_constraintEnd_toStartOf="@+id/btn_delete_item"
            app:layout_constraintStart_toStartOf="@id/tv_record_time"
            app:layout_constraintTop_toBottomOf="@id/tv_record_time"
            tools:text="지하 2층 A구역" />

        <!-- 4. 메모 (회색 내용) -->
        <TextView
            android:id="@+id/tv_record_memo"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginTop="4dp"
            android:layout_marginEnd="16dp"
            android:ellipsize="end"
            android:maxLines="2"
            android:textColor="#555555"
            android:textSize="14sp"
            app:layout_constraintEnd_toStartOf="@+id/btn_delete_item"
            app:layout_constraintStart_toStartOf="@id/tv_record_floor"
            app:layout_constraintTop_toBottomOf="@id/tv_record_floor"
            tools:text="입구 바로 앞, 기둥 뒤에 주차함" />

        <!-- 5. 삭제 버튼 (오른쪽 위) -->
        <ImageButton
            android:id="@+id/btn_delete_item"
            android:layout_width="24dp"
            android:layout_height="24dp"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:src="@android:drawable/ic_menu_close_clear_cancel"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            app:tint="#888888"
            tools:ignore="ContentDescription" />

    </androidx.constraintlayout.widget.ConstraintLayout>
</androidx.cardview.widget.CardView>